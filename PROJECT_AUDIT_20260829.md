# PA-STFed 可发表性导向核查与实验整理

## 结论摘要

SmartDS 文件中的 `adj` 在全部 273 个节点上形成 57 个连通分量。删除 181 个始终为零的节点后，92 个有效负荷节点的诱导子图退化为 92 个孤立点；因此，原先的“57 个分量”不是删除零负荷节点后才产生的，但删除确实会丢失零负荷节点作为拓扑中继的作用。

进一步核查得到：

| 图方案 | 节点数 | 无向边数 | 连通分量数 | 推断边数 | 说明 |
|---|---:|---:|---:|---:|---|
| `raw_full` | 273 | 216 | 57 | 0 | 原始图，含零负荷节点 |
| `active_induced` | 92 | 0 | 92 | 0 | 删除零负荷节点后的诱导子图，仅作诊断 |
| `projected_raw`（实验别名 `forest`） | 92 | 54 | 56 | 0 | 零负荷中继投影后的原始拓扑图；不预设无环 |
| `legacy_inf` | 273 | 272 | 1 | 56 | 旧的全节点 MST 方案 |
| `mst_projected` | 92 | 109 | 1 | 55 | 正式方案：先投影，再以有效节点为端点补边 |

原始图满足 \(216-273+57=0\)，数学上是 57 棵树组成的森林。该事实不能证明存在 56 条真实缺失线路。旧方案的 56 条桥接边中，0 条为有效节点—有效节点，1 条为有效节点—零负荷节点，55 条为零负荷节点—零负荷节点；因此旧 `inf` 方案在仅预测有效节点且 `hop_radius=2` 时几乎不能提供有效的推断边信息。零负荷中继投影会把同一零节点连通块的多个有效邻居两两相连，故投影图可能含环；“forest”仅保留为实验命令别名。

审计还统计了 92 个有效节点的无序节点对（共 4186 对）上的最短跳数。原始全图 \(\mathcal{G}^{raw}\) 只有 54 对可达（覆盖率 1.29\%，跳数中位数 3、P90=4、最大 5）；历史全节点 MST 图全部可达，但跳数中位数 26、P90=51、最大 65。中继投影图保留 54 对可达关系（均为 1 跳），修正后的有效节点 MST 图全部可达，跳数中位数 14、P90=29、最大 41。这里的跳数是图论关系，仅用于审计和注意力边先验，绝不是导线物理长度或阻抗。

正式代码将旧方案保留为 `legacy_inf`，新增：

- `forest`：保留原始有效负荷节点之间由零负荷节点中继形成的投影关系（正式定义名为 `projected_raw`，不预设无环）；
- `mst_no_tag`：在投影图上增加 55 条有效节点候选桥接边，但第三个边属性恒为 0；
- `mst_tag`：同样增加 55 条桥接边，并把候选边来源编码到第三个边属性。

## 数据流

1. 从 `smartds_graph.npz` 读取坐标、邻接矩阵、边索引、负荷序列和节点编号。
2. 用全时间序列识别 92 个有效节点；181 个零负荷节点不进入预测损失。
3. 在全图上保留零负荷节点的路径信息，构造 92 节点零负荷中继投影图。
4. 仅在投影后仍处于不同分量的有效节点之间执行确定性 Kruskal MST。
5. 以 15 分钟采样的过去 96 步作为输入，预测未来 12 步。
6. 集中式模型联合预测全部 92 个有效节点；联邦模型按空间划分为 8 个客户端，每个客户端约 10--13 个节点。
7. 客户端仅加载本地负荷窗口，服务器只接收允许共享的模型状态。

## 联邦参数共享边界

参数分组由 `model.py:17--23` 的 `LOCAL_PARAMETER_PREFIXES` 和 `shared_state_dict` 控制，训练和聚合位于 `federated.py:81--219`、联邦主循环位于 `run.py:1275--1650`；审计脚本会随代码更新实际行号说明。

| 参数/状态 | FedAvg/FedProx | Personalized | 依据 |
|---|---|---|---|
| `input_projection.*` | 共享、聚合 | 共享、聚合 | 输入特征到隐藏空间的通用编码 |
| `physical.*` | 共享、聚合 | 共享、聚合 | 边感知物理图注意力 |
| `spatial_gate.*`、`temporal_gate.*` | 共享、聚合 | 共享、聚合 | 门控融合参数 |
| `temporal.*` | 共享、聚合 | 共享、聚合 | 时间 Transformer |
| `head.*` | 共享、聚合 | **本地保留** | 个性化预测头实验 |
| `functional.value.*` | 共享、聚合 | 共享、聚合 | 功能图消息变换 |
| `functional.embedding_1/2` | **本地保留** | **本地保留** | 节点数随客户端变化，无法直接平均 |
| 非参数配置、图张量 | 不上传 | 不上传 | 每客户端本地构造 |

当前联邦实现交换的是模型状态，不是原始负荷窗口；这只能支持“原始数据不集中上传”或“数据留在客户端”，不能等同于差分隐私、加密或形式化保密保证。主实验关闭 DP，只有显式 `dp_fedavg` 才启用裁剪和噪声。

审计命令同时生成 `outputs/federated_parameter_groups.csv`，逐个列出
`state_dict` 条目及其 FedAvg/FedProx 与 Personalized 行为；JSON 中的
`federated_parameter_audit.code_locations` 给出对应源码位置。DP 结果还记录
`(C, sigma, q, R, delta, epsilon)`、均值噪声方差和 `privacy.py::gaussian_rdp_epsilon`
的源码版本标识。

当前 PA-STFed 实例共有 47 个 `state_dict` 条目：FedAvg/FedProx 聚合 45 个，
功能图节点嵌入 2 个始终本地保留；`personalized_head=true` 时再将 6 个 `head.*`
条目留在客户端，服务器只聚合其余 39 个条目。

## 三组生死实验

### 拓扑来源敏感性

固定 PA-STFed、时间切分、训练预算和随机种子，仅改变 `data.graph`：

| 实验名 | `data.graph` | 目标 |
|---|---|---|
| `topology_forest` | `forest` | 原始拓扑经过零负荷中继投影后的基线 |
| `topology_mst_no_tag` | `mst_no_tag` | 检验候选桥接关系本身是否有效 |
| `topology_mst_tag` | `mst_tag` | 检验显式推断边来源标记是否有效 |

不预设误差排序。只有在多种子配对区块 Bootstrap 的差异均值 95\% 置信区间不跨 0 时，才使用“优于”；否则使用“数值较低”或“表现接近”。若没有稳定改善，应将 MST 降级为敏感性分析；若 `mst_tag` 不优于 `mst_no_tag`，则边来源标记不是已证实的贡献。

### 物理图与功能图互补性

固定 `data.graph=mst_tag`，比较：

- `spatial_physical_only`：仅物理图；
- `spatial_functional_only`：仅本地功能图；
- `spatial_dual_graph`：物理图与功能图双图门控。

该组用于判断双图是否真的互补，不能只凭参数量或结构复杂度宣称有效。

### 联邦协同与个性化

比较 `local_only`、`fedavg`、`fedprox_005`、`personalized_head`。主要报告客户端宏平均 WAPE、RMSE、MAE、sMAPE、MAPE，以及标准差、P90/P95 和最差客户端。联邦实验回答的是在空间异构和数据留在客户端时的精度—协同折中，不预设联邦优于集中式。

## 统一评价协议

- 时间划分固定为 80%/10%/10%，不随机打乱。
- 模型选择只使用验证集；最终测试仅在锁定配置后运行。
- 所有关键配置至少使用种子 2026、2027、2028。
- 报告节点宏平均和聚合负荷指标，避免只呈现最有利口径。
- MAPE 使用训练集节点级低负荷阈值，并同时记录有效覆盖率。
- 所有“优于”结论应由配对时间块 Bootstrap 的差异均值和 95% 置信区间支持；没有显著性检验时使用“数值较低”或“表现接近”。

## 推荐执行顺序

1. `python run.py audit`：确认 `outputs/audit_report.json` 和 `outputs/topology_comparison.csv`。
2. 三个拓扑实验，先用一个种子做烟测，再运行三种子。
3. 三个物理/功能图实验，三种子。
4. `local_only,fedavg,fedprox_005,personalized_head`，三种子。
5. 锁定最佳方案后运行 `centralized_test`、`fedavg_test` 和必要的 `personalized_head_test`。
6. 最后整理每客户端尾部误差、门控分布和拓扑敏感性图表。

当前每个集中式任务最多 30 个 epoch、早停 patience=7；每个联邦任务最多 20 轮、早停 patience=5。完整三种子矩阵的主要成本来自联邦任务，不建议在生死实验前继续增加模型模块。

## 论文贡献边界

最可能成立的贡献是：

> 面向拓扑信息不完备且客户端负荷异构的 SmartDS 合成配电网，提出一种可审计的零负荷中继投影—候选桥接—边来源感知建模流程，并评估其与本地功能图及部分个性化联邦聚合的协同效果。

不能写成“首次使用联邦学习”“首次使用双图”“首次使用 MST 补边”或“实现隐私保护”。SmartDS 是合成数据，8 个客户端是空间划分模拟，MST 边是待验证的拓扑假设。

最可能失败的模块是：

- `mst_tag` 若不优于 `mst_no_tag`，来源标记应降级为实现细节；
- 双图若不优于单图，双图叙事应删除；
- FedProx 若不优于 FedAvg，不应强行保留为改进算法，只作为对照；
- 若节点级误差仍高而聚合误差较低，应将结论限定为系统级聚合负荷可行性，并如实报告节点级结果。
